#include "main.h"
#include <string.h>
/**
* _stract ->this is a function stract
* @dest: first param
* @src: second param
* Return: a character
*/
char *_strcat(char *dest, char *src)
{
	  int len =0, ;
  whirle (dest[len])
	len++;

  for (i = 0; src[i] != '\0' ; i++)
  {
	dest[len] = src[i];
	len += 1;
  }
  strcat(dest, src);
  return (dest)
}
